# 图片来源

当前使用的 `soundsticks5.png` 是此前从 HK One App 提取的 `ss5.45940214.png`，原始尺寸为 510 × 360。直接复制，未放大、抠图或重新编码。

本机原始资料保留在工作区 `app-assets-local/hk-one-original/`。第三方产品图片与品牌素材不代表授予任何额外使用许可。

App 的 normal / selected 灯效素材曾用于对比，现仅留作本机参考，当前卡片使用 CSS 渐变绘制，不依赖这些图片。
